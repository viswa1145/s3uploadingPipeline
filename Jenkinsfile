pipeline {
    agent any

    stages {
        stage("Maven Build") {
            steps {
                sh '''
                cd $WORKSPACE
                pwd
                '''
            }
        }
    }
}
